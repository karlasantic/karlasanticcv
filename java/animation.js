var options = {
    strings: ['Karla Šantić'],
    typeSpeed: 100
  }
  
  var typed = new Typed('.typed', options);
  var options = {
    strings: ['Karla Šantić'],
    typeSpeed: 80
  
  }
  
  var typed = new Typed('#typingAnimation', options);
